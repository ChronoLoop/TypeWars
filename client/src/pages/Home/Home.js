import React from 'react';
import GameMenu from '../../components/GameMenu/GameMenu';

const Home = () => {
    return (
        <section className="mt-5 p-5">
            <GameMenu />
        </section>
    );
};

export default Home;
